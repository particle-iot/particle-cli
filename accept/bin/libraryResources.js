/**
 * Outputs the location of the library test resources provided by the library manager package.
 */
var libraryTestResources = require('particle-commands').libraryTestResources;
console.log(libraryTestResources());
